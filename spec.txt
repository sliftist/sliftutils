

IMPORTANT! modified time PLUS size is what's important. Store both!


Running...
**1) Moving to a fully RemoteConfig system
    So this is my idea for the new spec. Could you see if there's any holes in it? Write up a spec of the changes that we need to make and any questions you have. 

    I added "RemoteConfig", which uses "RemoteConfigBase" in IArchives.ts.  What we want to do is we want to have it so that this is our new base config type.  In the remoteStorage folder, We need a function that will take this format and use it to create an IArchives compliant interface. It should take RemoteConfig | RemoteConfigBase (because RemoteConfigBase can be turned into RemoteConfig just by wrapping it in an array).


    At the same time, we need to redefine how we're storing the configuration for our data. We will of course lazily do our setup. So just creating something with the remote configs shouldn't actually do anything. But then when a function is called and we do lazily do our setup, we need to then check all of the sources, checking the storage routing JSON file in all of the sources, and trying to update it if our version is newer. And if our version isn't newer, then we should get the latest configuration. This might immediately change the list of configuration sources that we have. However, we shouldn't then try to get the configuration for those configuration sources. We should only do one level of getting storage routing JSON, and then we're done. We use the one returned that has the newest version. And we only write if our version that was passed into our constructor is newer. If we find a version that's newer than some of the other ones, we won't write to them.

    And, furthermore, on our own hosted database, Instead of separately storing the bucket configurations, the bucket configurations should be stored inside the bucket itself, inside of the storage folder in the storage routing JSON file. If an attempt is made to access a bucket that doesn't exist, instead of throwing, we should just be returning undefined as all the files are undefined. And if a write is made, if the write is to the this storage routing JSON file, then that write should be allowed, And this should cause us to actually create the bucket, with that configuration. When we create the bucket, we'll be creating the blob store, which if it has data that it needs to synchronize, it might immediately start scanning. And then of course anytime this file is written to, we need to update our configuration, which might change how we're scanning, etc. We might need to cancel ongoing scans. And furthermore, If we pull in the configuration file during synchronization, which is completely possible, we need to then respect this file. Of course, we'll only pull it in if it has a newer write time. So it should always mean we're pulling in the latest one, and we should never end up getting into some weird loop. AND, Hopefully it will only add synchronization sources in the future with different right write valid times and remove ones in the past, which we've already stopped synchronizing from. So it should be fairly smooth. This simplifies our API as now setting up buckets won't require an extra API call.

    You should update our code, make sure the latest code is running, and then update our example code as well.

    Also, make it so if cloudflareApiToken is not provided we fall back to getSecret("cloudflare.json")



1) We should set up the bootstrapper locally and even test it on a storage server locally, I think? Might as well Should be easy to do. We can test the storage server just running it in Slift utils. Doesn't need to share code, that's kind of the whole point. 
2) Set up a bootstrapper that we can chain with another command that will set up secrets, cloudflare key, etc
    yarn exposesecrets && yarn storageserver --url https://storage.vidgridweb.com:4446 --folder ~/storagetest
    - The bootstrapper needs to write to "cloudflare.json" and "backblaze.json"
        - Using our sync server secrets. Not sure if they're stored in there, but, you know, easy enough...





This is all done?
        todonext
        UGH... Wait, so should we just have it so the client tries to update the configuration data itself? And then our server doesn't even have an API, it just polls that configuration?
            AND, We write to all of the sources? And really we write the full configuration to all the sources, So it's heavily redundant. Any source has the full configuration. And this is ignoring the write back etc. considerations we always write it (Unless of course we don't have write access. If we're just in read mode, then it's different).


        - We need to update our HTTP handling so this is supported:
            https://storage2.vidgridweb.com:4445/file/querysubtest-com-public-immutable/storage/storagerouting.json
            - To do this, we're going to need to make a wrapper for require controller and make that the default call. I believe we will have access to the underlying HTTP call information so we can get the original URL, and full the pieces (the bucket name and path) out of it.




        OH! The functions for the construction need to be actually asynchronous. lambdas, or at least support that, because the credentials for backblaze might be secrets. 

        I think we just need to tell it to have one consolidated factory
            - I mean, I guess it will be two stages so we can make a factory function, so the configuration is simpler
            - With different types depending on the type of the configuration



        0) Consolidated sources So we have one single factory, and all the different types of sources are in a folder nicely organized

            https://storage2.vidgridweb.com:4445/storagerouting.json
            https://f002.backblazeb2.com/file/querysubtest-com-public-immutable/storage/storagerouting.json
            local:transaction:trust
            local:direct:${bucketName}

            AH! But the configuration is really a list of objects with URLs, although there's a few different forms. 
                (FullConfig|string)[] | string
                Where string just becomes [string] immediately

            The first option has defaults of:
                { cacheReads: true, required: true }
            The rest have no defaults (So we're just a reader for it. So we write back to it, but we don't copy all the files).

            Types of sources
                Remote archives
                local singletons
                Remote backblaze API
                Remote backblaze URL only (read only)


        -2.1) Rename some of the stuff like blob store so everything makes more sense for what it actually does. 

        1) Verify the code and make updates
        2) VERIFY that our scanning code is efficient and not download something that we already have. 



        -1) Update it so that the bucket configuration has a version number in it. That way, old code doesn't force an old bucket configuration. We'll check and we'll see, oh, well, we do want to ensure that it has this bucket configuration, but the versions outdated. So instead, we'll read the bucket configuration from the remote, instead of updating it.


        0) CLI configuration of bucket configuration source (and special option so WE are the source). This sets the default bucket configuration. So if a user tries to set something with an older version, this will take over. And when the user sets something with a newer version, we will write to this source.
            - Global configuration per bucket stored in our root source, hosted on a URL, Used as our source of truth about where our sources come from, with API functions to update this. We can also have this store our bucket configuration with the version as well. So, our code with the newer version will still clobber this, or if this is newer than our code, it will clobber it. It just depends on which one we update.
            - CONFIGURED via the remote factory (still per bucket I guess)
        AH!, Update it so you can set any bucket settings except the bucket name in the factory or in the bucket. So you can add defaults if you want. For example, the bucket configuration source. 

        1) Support configuring synchronization sources in bucket config (although we will probably usually set it in the remote factory).
            - The sources will all be global. They won't be namespaced. 
            - The sources will all be full URLs

        2) use the global configuration in readers in case the main synchronization source is down.
            - I believe we can just always use it. Even if it's out of date, it's probably better than nothing. 
            - BUT, never write to anything but the main source!

        2) MAKE SURE! That we just have something that goes from an array of URLs that has the config.json (perhaps with object configuration as well, Although we should allow it to just be a list of URLs, using default sync options), And from this it can make a reader.
            - We'll both use this client side and server side. Although server side, we will do a few extra things. Like, we'll see, oh, that URL is ourself, So we don't need to talk over HTTP. We can just use the singleton in memory, etc. 

        3) Test on our test site, Running two processes and having one chain to the other. And then play around a bit writing and then taking down one and making sure we can still read from the other, etc., etc. 




        1) Allow specifying storage sources to store our credentials
            - Check if it's in public mode and if it is throw, we can't allow our credentials to be public
            - DEFAULT to use ourself


        2) Support backblaze urls, using our credential store to access them
            - If they don't exist, we'll request their credentials, which will require going onto the site to specify the backlash credentials, which will then be stored in our credential source, which defaults to ourself. 

        3) Verify we can use Backblaze as a synchronization source now and fall back to it for reading. 




2) Use our machine configuration to setup a storage service

2.1) cyoa page to setup services + VIEW their storagerouting.JSON
    WE DO NOT want to configure it via the interface! We want the configuration to always be in code. That way, we can update one place and all of our buckets will update!
    OH! Make something that will import all of our code just like in a deploy, and then we'll gather all of the bucket configurations that we created using our helper functions so we register them all, and then we'll touch them all to make sure the configuration is updated on all of them!
    - We should probably make a factory so we can easily create buckets without having to find the full chain every single time, and so we can change the full chain easily in one place. 
    - We should allow easily chaining them for redundancy
    - We might even have some UI that shows all of the storage services together, allowing you to easily configure how they chain, etc. 
    - We should also have it poll for the status of like scanning file stats, etc. 

2.2) Test some of these custom buckets on CYOA (local dev), testing redundancy, speed, etc.
    - If we run into a lot of issues that we need to debug, we can always host the storage server locally. 

    custom => backblaze
        - So internal redundancy and then also backblaze at the very end
2.3) Test using our thing as a front end for back plays with no copy, which should mean there's really no setup. 
    - Just test reading images or something. 







3) Support sharding in synchronization sources as well, adding new field
    route: {
        range: [number, number];
        level: number;
    }
    - Route level is required as if two things have different route ranges, they obviously aren't going to be pulling from each other. So we need another way to stack redundancy, and the route level can do that. If two things are at the same route level, it means they aren't using each other for redundancy. We could automatically detect this, but it's more clear if it's explicit.

3.1) We will almost always set up copy files on our different shards, but of course, these will only be considered depending on the valid window. 
3.2) Of course, having our site configuration support sharding as well. So you don't need to fiddle with the shards. You kind of just say there are a certain number of shards. And if you want to change the shards, it will automatically set them up in the future, applying a default switch time of a day that you can also configure yourself, etc. 




5) Shard retry writes - Basically, they give us part of a path, and part of it we can fill in ourselves. We try to keep track of which shards are down and if a shard is down, we will keep changing that random value until we find something that doesn't match that shard Until we hit a certain percentage of shards being down, probably greater than 99%. And then we return the path that we ended up using. 
    - This solves the problem of not being able to write to the backup sources. As it'll mean you'll be able to read if a shard is down, and you'll also be able to write if a shard is down. So it'll be like everything's fine. 
    - We only need to use this for certain important things like uploading images. pretty much all of the AI stuff, we can probably use it. 
    HMMM!!! This causes problems if we want it to be an image or a video. So we should probably have it so by default we try to put nothing there, So it'll be the exact path the user requested. That way, if they try to do the same right multiple times, it'll clobber itself.
        - Although realistically, I don't think it'll really matter.  because we're not generating the same content most of the time. 
6) geographical sharding as well. Which tries to change the hash so it's the server that we're closest to
    - This can massively reduce the write latency, Which is important because we don't want to have to send the data around all the time, we just want to send around the URLs. Well then this requires waiting for the remote to confirm that we can read back. which will be purely limited really on latency if it's close to us. 

7) readerDiskLimit. Only when our first sync source has copyFiles is false and cacheReads is true (and required true). Once we copy over a certain amount, we start deleting the least recently used from our local cache. This allows us to cache large sources without getting overwhelmed. 





ArchivesRemote
    bucketName
    public
    fast
    writeDelay

ArchivesFactory
    host: https://storage.vidgridweb.com:4444/storagerouting.json
    redundantHosts:
        - https://storage2.vidgridweb.com:4445/storagerouting.json
        - https://f002.backblazeb2.com/file/querysubtest-com-public-immutable/storage/storagerouting.json

HostStorageServerConfig
    host: https://storage.vidgridweb.com:4444/storagerouting.json
    redundantHosts:
        - https://storage.vidgridweb.com:4444/storagerouting.json
        - https://storage2.vidgridweb.com:4445/storagerouting.json
        - https://f002.backblazeb2.com/file/querysubtest-com-public-immutable/storage/storagerouting.json
    folder: os.homedir() + "/storage"
    cloudflareApiToken: ...


https://f002.backblazeb2.com/file/querysubtest-com-public-immutable/image/img_00BloAFyIevA_1024x1536.jpg
